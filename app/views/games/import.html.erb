<h1>Where would you like to import armies?</h1>
<%= render :partial => "svg/board", locals: { factories: @factories, regions: @eligible_regions, game: @game.id, pieces: @pieces, turn: 'import', import_count: session[:import_count] || 0 } %>
